const fs = require('fs');
const path = 'apps/web/src/pages/DashboardPage.tsx';
let text = fs.readFileSync(path, 'utf8');
const startMarker = '      {activeTab === "general" ? (\r\n';
const footerMarker = '\r\n    </div>\r\n  );\r\n}\r\n';
const start = text.indexOf(startMarker);
if (start === -1) throw new Error('start marker not found');
const footerIndex = text.indexOf(footerMarker, start);
if (footerIndex === -1) throw new Error('footer marker not found');
const endSearchStart = text.lastIndexOf('      )}\r\n', footerIndex);
if (endSearchStart === -1 || endSearchStart < start) throw new Error('end marker not found');
const replacement = `      {activeTab === "deposits" ? (\r\n        <div className="sales-deal-deposits">\r\n          <section className="sales-deal-deposits-hero">\r\n            <div className="sales-deal-deposits-hero-copy">\r\n              <div className="sales-deal-deposits-title-row">\r\n                <div>\r\n                  <div className="sales-deal-deposits-kicker">Deposits</div>\r\n                  <h4>Payment Center</h4>\r\n                </div>\r\n                <span className={\`sales-deal-deposits-status is-\${dealRow ? "pending" : "posted"}\`}>{depositStatus}</span>\r\n              </div>\r\n              <p>Kennect-inspired capture with the deal context still in view. Use the global <strong>Take Deposit</strong> action to open the payment workflow.</p>\r\n            </div>\r\n            <div className="sales-deal-deposits-summary-grid">\r\n              <div className="sales-deal-deposits-summary-card"><span>Requested</span><strong>{formatWorkbenchCurrency(depositAmount)}</strong></div>\r\n              <div className="sales-deal-deposits-summary-card"><span>Captured</span><strong>{formatWorkbenchCurrency(depositCollected)}</strong></div>\r\n              <div className="sales-deal-deposits-summary-card"><span>Remaining</span><strong>{formatWorkbenchCurrency(depositRemaining)}</strong></div>\r\n            </div>\r\n          </section>\r\n\r\n          <div className="sales-deal-deposits-layout">\r\n            <section className="sales-deal-deposits-ledger">\r\n              <div className="sales-deal-deposits-panel-header"><h4>Deposit Ledger</h4><span>{dealRow ? 1 : 0} record{dealRow ? "" : "s"}</span></div>\r\n              <div className="sales-deal-deposits-table-wrap">\r\n                <table className="sales-deal-deposits-table">\r\n                  <thead>\r\n                    <tr><th>Deal</th><th>Customer</th><th>Request</th><th>Amount</th><th>Status</th><th>Reference</th></tr>\r\n                  </thead>\r\n                  <tbody>\r\n                    {dealRow ? (\r\n                      <tr>\r\n                        <td>{dealRow.id}</td><td>{customer}</td><td>{date}</td><td>{formatWorkbenchCurrency(depositAmount)}</td><td>{depositStatus}</td><td>{depositReference}</td>\r\n                      </tr>\r\n                    ) : (\r\n                      <tr><td colSpan={6}>Select a deal to view deposit requests.</td></tr>\r\n                    )}\r\n                  </tbody>\r\n                </table>\r\n              </div>\r\n            </section>\r\n\r\n            <section className="sales-deal-deposits-rail">\r\n              <div className="sales-deal-deposits-card">\r\n                <div className="sales-deal-deposits-panel-header"><h4>Deposit Activity</h4><span>{depositActivity.length} items</span></div>\r\n                <div className="sales-deal-deposits-activity-list">\r\n                  {depositActivity.map((activity) => (\r\n                    <article className="sales-deal-deposits-activity-item" key={\`${'${'}dealWindow.dealId${'}'}-\${'${'}activity.title${'}'}\`}>\r\n                      <div className="sales-deal-deposits-activity-copy"><strong>{activity.title}</strong><p>{activity.detail}</p></div>\r\n                      <span>{activity.meta}</span>\r\n                    </article>\r\n                  ))}\r\n                </div>\r\n              </div>\r\n            </section>\r\n          </div>\r\n        </div>\r\n      ) : (\r\n        <div className="sales-deal-tab-placeholder">\r\n          <p>{salesDealTabs.find((t) => t.id === activeTab)?.label ?? "Tab"} — Coming soon.</p>\r\n        </div>\r\n      )}\r\n`;
text = text.slice(0, start) + replacement + text.slice(endSearchStart + '      )}\r\n'.length);
fs.writeFileSync(path, text, 'utf8');
console.log('replaced sales tab chain with deposits-focused branch');
